---
title: Workspace Studio
sidebar_label: Workspace Studio
sidebar_position: 6
hide_title: true
---

import '../_ai-kit.css';

<div className="ai-landing">

<header className="ai-head ai-wrap">
  <p className="ai-eyebrow ai-reveal">AI Assistant</p>
  <h1 className="ai-head__title ai-reveal">Workspace Studio</h1>
  <p className="ai-lead ai-reveal">Get direct, code-level access to a workspace's underlying files — components, data sources, and configuration — instead of working through panels and settings screens.</p>
</header>

<section className="ai-section ai-wrap">
  <p className="ai-eyebrow">What it helps with</p>
  <ul className="ai-list">
    <li>fixing a bug in a specific component</li>
    <li>writing or editing a function used by a grid, form, or board</li>
    <li>adding a column, field, or data transform that needs custom logic</li>
    <li>reviewing exactly what changed in a workspace before saving it</li>
  </ul>
</section>

<section className="ai-section ai-wrap">
  <p className="ai-eyebrow">Try asking</p>
  <div className="ai-chips">
    <code className="ai-chip">"Fix the bug in this grid's column setup"</code>
    <code className="ai-chip">"Add a new column to this component"</code>
    <code className="ai-chip">"Help me edit this function so it filters by status"</code>
    <code className="ai-chip">"Show me what changed in this workspace since I opened it"</code>
  </div>
</section>

<section className="ai-section ai-wrap">
  <p className="ai-eyebrow">When it fits</p>
  <div className="ai-duo">
    <div className="ai-callout ai-callout--do">
      <p className="ai-callout__label">Reach for it when…</p>
      <ul className="ai-list">
        <li>fixing a component that behaves incorrectly</li>
        <li>adding custom logic to how data loads or displays</li>
        <li>building a feature that isn't covered by standard widget options</li>
        <li>reviewing the underlying files of a workspace to understand how it works</li>
      </ul>
    </div>
    <div className="ai-callout ai-callout--dont">
      <p className="ai-callout__label">Use another assistant when…</p>
      <ul className="ai-list">
        <li>If you want to add or resize a panel, or change a filter or widget setting, without touching code — <a href="/docs/ai/agents/workspace-agent">Workspace Assistant</a></li>
        <li>If you only want help with text — <a href="/docs/ai/agents/writing-agent">Writing Assistant</a></li>
      </ul>
    </div>
  </div>
</section>

<section className="ai-section ai-wrap">
  <p className="ai-eyebrow">What to have ready</p>
  <ul className="ai-list">
    <li>the workspace you are working in</li>
    <li>the specific component or file, if you already know it</li>
    <li>a clear description of the bug or behavior you want to change</li>
    <li>any error message you are seeing</li>
  </ul>
</section>

<section className="ai-section ai-wrap">
  <div className="ai-callout ai-callout--tip">
    <p className="ai-callout__label">What to expect</p>
    <p className="ai-callout__body">Workspace Studio reads the relevant files first, explains what it plans to change, and then applies the change directly. It is built for users comfortable working close to the code, such as studio leads or technical admins — for everyday layout and widget changes, the <a href="/docs/ai/agents/workspace-agent">Workspace Assistant</a> is the faster path.</p>
  </div>
</section>

</div>
